# Session Handoff: R3 Q1 Multi-view Comparison Figure

## 任务目的
为 Reviewer #3 Q1 (Insufficient qualitative evidence for multi-view 3D consistency) 制作多视角对比图并更新 rebuttal 回答。

## 执行内容
- 修改 `figures_rebuttal/make_mv_comparison_fig.py`：
  - 配置三组样本：00250 (v4/v6/v1)、04198 (v3/v6/v0)、03360 (v4/v7/v2)。
  - 移除顶部 sample ID header（SAMPLE_HEADER_H = 0）。
  - 移除 View 1/2/3 列标签（VIEW_HEADER_H = 0）。
  - 最终输出：`figures_rebuttal/mv_comparison_00250_04198_03360.png`，2198 × 684 px。
- 在 `rebuttal.tex` 中 R3 Q1 后插入单栏 `figure[t]`（非跨栏 `figure*`），引用该图。
- 将 R3 Q1 回答改为简洁正面版本，不先承认问题，直接指向新图并给出结论。

## 最终 R3 Q1 文本
```latex
Thanks for the advice. In Fig.~\ref{fig:mv_comparison}, we provide additional multi-view comparisons. Compared with Trellis, our model maintains more consistent and natural color tones and semantic contents across viewpoints. We will include more high-resolution examples in the next version.
```

## 图片 Caption
```latex
High-resolution multi-view comparison between Trellis and OREO. OREO better preserves object identity and texture details across side/back views.
```

## 关键决策
- 不用 `figure*` 跨栏，使用单栏 `figure` + `\linewidth`。
- R3 Q1 不以 "We agree..." 开头承认不足，而是直接 "Thanks for the advice" + 正面展示结果。
- 强调 **色调一致性 (color tones)** 和 **语义内容一致性 (semantic contents)**，而非 geometry inconsistency。
- 不说 "large-scale multi-view results"（只有3个例子，不宜过度承诺）。

## 参考文件
| 文件 | 说明 |
|------|------|
| `figures_rebuttal/make_mv_comparison_fig.py` | 生成多视角对比图的脚本 |
| `figures_rebuttal/mv_comparison_00250_04198_03360.png` | 最终输出图片 |
| `figures_rebuttal/mv_comparison/` | 原始渲染数据目录（00250, 03360, 04198 等） |
| `rebuttal.tex` L86–L96 | R3 Q1 回答 + figure 环境 |

## 当前 rebuttal 状态
- 编译成功，2 页 PDF。
- 两张图：mv_comparison（R3 Q1）和 view_edit_traj/test_09（R3 Q3），均在第2页。
- `\showreviewtagstrue` 仍开启（最终提交前需关闭）。

## 下一步任务
- 润色 Reviewer #1 和 Reviewer #2 的 rebuttal 回答。
- 可能需要检查篇幅是否仍在 2 页限制内。
- 最终提交前切换 `\showreviewtagsfalse`。
