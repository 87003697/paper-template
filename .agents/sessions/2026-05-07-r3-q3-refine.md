# Session Handoff: R3 Q3 Rebuttal Refinement

## 任务目的
本 session 主要 refine Reviewer #3 关于 Sec. 3.3 / Eq. 7 / negative source guidance 的 rebuttal 回答，并围绕 `figures_rebuttal/view_edit_traj/test_09.png` 组织更直观的 intermediate-step analysis。

## 执行内容
- 阅读此前 R3 Q2/Q4 handoff、当前 `rebuttal.tex` 和 `main.tex` 中 FlowEdit / Reinforced Editing / Eq. 7 的相关描述。
- 确认 reviewer 的真实问题是 “Explanation of Eq. 7 is confusing”，并希望通过 FlowEdit intermediate edit steps under negative source guidance 增强直觉。
- 多轮讨论后放弃过长的 push-away / pull-toward 公式解释、pose/viewpoint drift 解释、source condition unknown 解释，以及 over-align / local optimum 等说法。
- 确定 R3 Q3 采用更短的机制解释：negative source branch acts as an auxiliary regularizer, stabilizing reference-guided editing。
- 将 `figures_rebuttal/view_edit_traj/test_09.png` 加入 rebuttal figure，用于展示 w/o negative branch 会产生 unstable global color changes 和 weaker local structures。
- 将 R3 Q3 标题缩短为 “Explanation of Eq.~7 is unclear.”，避免标题里重复 negative source guidance。
- 更新 LaTeX Workshop workspace 设置：`autoBuild.run` 改为 `onFileChange`，并补充 PATH 以解决 `env: perl: No such file or directory`。
- 在 `rebuttal.tex` 顶部加入 `% !TEX root = rebuttal.tex`，减少 LaTeX Workshop 编译错 root file 的风险。
- 多次手动运行 `/Library/TeX/texbin/latexmk -synctex=1 -interaction=nonstopmode -file-line-error -pdf rebuttal.tex`，确认 `rebuttal.pdf` 可正常生成。

## 调试经验
- LaTeX Workshop 输出显示它确实能成功编译 `rebuttal.tex`，但 agent 外部写入文件后 watcher 不一定会自动触发；需要手动 build 或 reload Cursor window 后再观察。
- `env: perl: No such file or directory` 是因为 LaTeX Workshop 进程 PATH 找不到 `/usr/bin/perl`；在 `.vscode/settings.json` 的 tool env 中加入 `/usr/bin` 等系统目录后解决。
- 对 R3 Q3 不宜说 “avoid copying source appearance”，因为 OREO 不是要保留低质量 source appearance，而是让 reference-guided editing 更稳定。
- 不宜说 w/o negative branch 是 local optimum 或 over-align to reference；从当前图看更稳的分析是 unstable global color changes and weaker local structures。
- “while using it better preserves ...” 最终删掉，因为 figure 本身已经展示 ours，对 rebuttal 来说保持短句更好。

## 参考代码
| 文件 | 关键位置 | 说明 |
|------|---------|------|
| `rebuttal.tex` | Reviewer #3 Q3 | 当前 R3 Q3 的最终短版回答和 trajectory figure。 |
| `figures_rebuttal/view_edit_traj/test_09.png` | 全图 | with/without negative source guidance 的 intermediate editing trajectory。 |
| `main.tex` | Sec. 3.3 / Eq. 7 / Algorithm Reinforced Editing | FlowEdit coupled trajectory、negative guidance、noise update 的论文上下文。 |
| `.vscode/settings.json` | LaTeX Workshop recipe | 项目级 latexmk 配置、PATH 修复和 `onFileChange` 自动构建设置。 |

## 最终方案
R3 Q3 最终采用极简回应：先感谢 reviewer，再说明 negative source branch in Eq. 7 acts as an auxiliary regularizer, stabilizing reference-guided editing；随后用 Fig. 说明移除该项会导致 unstable global color changes 和 weaker local structures；最后承诺在 Sec. 3.3 中扩展这部分 discussion 和 visualization。这样避免过度公式化，也直接回应 reviewer 要求的 visualization and analysis。

## 下一步任务
用户接下来会继续讨论 rebuttal 的其他问题，可能继续围绕 Reviewer #3 或其他 reviewers 的 Q&A 做措辞压缩和覆盖检查。

## 初步方案
- 继续从 `rebuttal.tex` 当前可见 PDF 版面出发，优先检查篇幅、Q 标题是否准确、每个回答是否有直接回应 reviewer 的原话。
- 若继续处理 R3，重点关注 Q1 qualitative evidence / multi-view consistency 与 Q4 computational cost / edit prompts 是否足够具体。
- 保持 R3 Q2 已确定的 trade-off 叙述，不轻易改回 prompt patch 或过细数值。
- 每次修改后手动编译 `rebuttal.tex`，因为 LaTeX Workshop watcher 对 agent 写入不一定稳定触发。
- 最终提交前记得将 `\showreviewtagstrue` 切换为 `\showreviewtagsfalse`，隐藏临时 coverage/status 标记。
