# Session Handoff: R3 Q2/Q4 Rebuttal Refinement

## 任务目的
本 session 主要 refine Reviewer #3 的 rebuttal 表述，重点处理 R3-Qual2 和 R3 Q4，并为下一步完善 R3-Clar1/Q3 以及其他 reviewers 的 rebuttal 留下上下文。

## 执行内容
- 讨论并重写 R3-Qual2：将原先过度归因于 prompt/color unconstrained 的说法，改成局部颜色一致性与整体 enhancement 的 trade-off。
- 确定 rebuttal 用词风格：短句、直接回应 reviewer 具体观察、承认局部现象但强调整体趋势、避免过度承诺和过细数值。
- 阅读 `main.tex` 中 FlowEdit/Reinforced Editing 相关部分，明确 R3-Clar1 当前解释不够准确，尤其不应说 negative source guidance 是为了避免 copy source appearance。
- 为 GPU 服务器测试脚本整理了中文 prompt：对比 with/without negative source guidance 的 intermediate editing trajectory，固定 input/seed/noise/guidance/steps，只改变 negative source guidance。
- 重写 R3 Q4：去掉 typo 相关回应，标题改为 “Computational cost and edit prompts”，只保留 renderer/decoder 反传约 10GB VRAM 和 edit prompt 说明。
- 多次编译 `rebuttal.tex`，`rebuttal.pdf` 均正常生成。

## 调试经验
- R3-Qual2 不宜写成 “加 white background prompt 会进一步提高质量”，这会显得主结果依赖 prompt patch；更稳的说法是 color-preservation prompts can reduce color shift but may suppress enhancement。
- R3 Q4 里 typo 不必重复回答，因为 rebuttal 开头已经整体回应过 typos；这里保留会浪费空间并分散重点。
- `batch size 1`、具体 CLIP/DINO 数值、`to ensure a fair comparison` 等细节都被删掉，以保持 rebuttal 简洁。
- 当前 R3-Clar1 仍未正式改写，需等 negative source guidance 轨迹可视化结果或至少按更准确机制重写。

## 参考代码
| 文件 | 关键位置 | 说明 |
|------|---------|------|
| `rebuttal.tex` | Reviewer #3 Q2--Q4 | 当前已更新 R3-Qual2 和 R3 Q4；R3 Q3/R3-Clar1 仍需重点完善。 |
| `main.tex` | Sec. 3 FlowEdit/Reinforced Editing | Eq. for FlowEdit coupled trajectories、Eq. 7 Reinforced Editing、noise update、RE qualitative/ablation 解释。 |
| `review/Reviewer#3.png` | Clarity bullets | R3-Clar1 原始要求：解释 Eq. 7，并用 FlowEdit intermediate edit steps under negative source guidance strengthen intuition。 |

## 最终方案
R3-Qual2 采用 trade-off 叙述：w/o noise update 在该例中颜色更稳是因为 editing 更保守；`keep consistent foreground colors` 这类 prompt 可减少 color shift，但可能 suppress enhancement；主设置使用 less restrictive prompt，因为 overall quantitative scores 更好。

R3 Q4 采用极简回应：renderer/decoder 反传约需 10GB VRAM；edit instructions 使用 prompts akin to Photo3D for both Qwen-Image-Edit and NanoBanana，并会在 revision 给 exact prompt text。

## 下一步任务
用户下一步要完善 R3 Q3 / R3-Clar1，并继续检查和 refine 其他 reviewers 的 rebuttal。

## 初步方案
- 优先改写 R3-Clar1：解释 FlowEdit coupled trajectories + same reference condition + opposite guidance scales，不要说 “avoid copying source appearance”。
- 如果 GPU 脚本结果可用，将 rebuttal 写成 “we will add/editing trajectories with and without negative source guidance”，强调该项 reduces layout/pose drift while allowing reference-related enhancement。
- 若暂时没有图，也可先改成机制更准确的短版：source branch anchors rendered view；velocity difference cancels common layout/pose/geometry motion；negative scale plus subtraction reinforces reference-conditioned guidance。
- 之后检查 R1/R2/R3 的 coverage tags 和 `\reviewstatus{}`，确认所有 major/clarity concerns 都有实质性回应。
- 最终提交前将 `\showreviewtagstrue` 改为 `\showreviewtagsfalse`，隐藏临时 coverage/status 标记。
